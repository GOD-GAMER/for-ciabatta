# Web / QR Endpoints

Server: aiohttp, started with the bot.

Endpoints
- /leaderboard — HTML leaderboard sorted by XP
- /recipes — Simple page linking to recipes
- /qr?url=<link> — PNG QR code for any link

Notes
- Default: http://localhost:8080
- Change host/port in .env (WEB_HOST/WEB_PORT)
